import Config

# Configure Ecto adapter for production (PostgreSQL)
config :phoenix_csv, :ecto_adapter, Ecto.Adapters.Postgres

# Note we also include the path to a cache manifest
# containing the digested version of static files. This
# manifest is generated by the `mix assets.deploy` task,
# which you should run after static files are built and
# before starting your production server.
config :phoenix_csv, PhoenixCsvWeb.Endpoint,
  cache_static_manifest: "priv/static/cache_manifest.json"

# Email is handled via Resend API directly - no Swoosh needed

# Do not print debug messages in production
config :logger, level: :info

# Runtime production configuration, including reading
# of environment variables, is done on config/runtime.exs.
